# Precisely MCP Server - Production Dependencies
# Minimal dependencies for production MCP server operations

# MCP Protocol Support
mcp>=1.0.0

# HTTP Client for API Calls
requests>=2.32.0

# Environment Variable Management
python-dotenv>=1.0.0

# Core Python Dependencies
typing-extensions>=4.0.0

# HTTP Transport Dependencies (optional - only needed for --transport http)
starlette>=0.27.0
uvicorn>=0.23.0
sse-starlette>=1.6.0
anyio>=4.0.0
